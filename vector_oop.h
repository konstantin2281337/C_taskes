#include<cmath>

using namespace std;

class Vector {

public: // ??????????? ???????
	void vector_point(int a, int b, int a1, int b1) { // ????????? ???????? ? ???????
		x1 = a;
		y1 = b;
		x2 = a1;
		y2 = b1;

	}



	void len() { // ?????? ??????? 
		cout << "len: " << sqrt(pow(x2 - x1, 2) + pow(y2 - y1, 2)) << endl; // ????? ??????? ?? ??????? 

	}



	int distance(int x1, int x2, int y1, int y2) { // ??????? ?????????? ?? ????? ?? ?????, ????? ????? ?????????? ?? ????? ?? ??????? 
		return sqrt(pow(x2 - x1, 2) + pow(y2 - y1, 2));
	}



	void distance_point_to_vector(int p_x, int p_y) { // ?????????? ?? ??????? ?? ?????
		cout << sqrt(2 * (pow(distance(p_x, x1, p_y, y1), 2)) +
			2 * (pow(distance(p_x, x2, p_y, y2), 2)) -
			pow(sqrt(pow(x2 - x1, 2) + pow(y2 - y1, 2)), 2)) / 2; // ?????????? ?????????? ????? ??????? ????????????

	}


private: // ??????????? ???????
	int x1, y1, x2, y2;

};

